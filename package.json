{
  "name": "ep-hardhat",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx hardhat run main.js --network localhost",
    "compile": "npx hardhat clear && npx hardhat compile",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.3"
  }
}
